    不再考虑 QML 端的 UI 适配，我们需要大幅度的重构
    同时对 api （app/server）, 前端(link_project/Figmaaidefectdetectionsystem) 修改, 不对数据进行过多的 适配
    1，对  app/server/db/models 中 的几个关键 表 添加 comment 中文注释  （我输出了目前不需要的表）
    2,api 输出与保留：
        移除重复功能的api,所有返回偏 数据库的原始数据 （可以对名称上进行统一）
        目前移除前端未使用的api
    3:前端数据加载流程，缺陷数据的请求，增加返回 图像数量，图像尺寸（宽度，高度） （分表面） 的数据来辅助 缺陷分布图的绘制
    缺陷分布图绘制：
        分布图是前端重要的功能之一，leftInImg，rightInSrcImg，topInSrcImg ... 等描述了位于图像的位置信息（x1,x2,y1,y2）,
        imgIndex 则是描述了 所在的图像索引，。根据 图像尺寸可以计数出位置，在web端  分布图是从左往右看的，而数据库是从上往下保存的。
            所以：
                1，绘制出缩略的矩形 （钢板图像大小的缩略）
                2，将缺陷按照 （x1,x2,y1,y2） 的值进行绘制上去
                3，如果显示 全部表面，则上下排列，如果钢板图像太长，限制最小绘制宽度（在图像上是宽度方向），然后通过滚动条横向滚动
            上下表不可单独滚动
            注意我需要的旋转

缺陷位置的变化关系：
    在分布图上，根据 数量，单张图像的尺寸绘制出的矩形，根据 imgIndex leftInImg，rightInSrcImg，topInSrcImg （x1,x2,y1,y2） 可以计算出 百分比的位置，然后根据 0点与钢板绘制宽高得到位置