在 网页界面中， 缺陷 -> 缺陷大图（瓦片加载的那个），修改如下：
    1：不再同时显示上下表面，上下表，全部按钮 只对缺陷列表，缺陷分布图进行显示控制
    2：默认按照100% 缩放显示（原始尺寸） 即便没有选中缺陷
缓存修改：
    服务端 优化图像缓存逻辑，缺陷读取 按照 缺陷命中->未命中时对应图像命中->未命中则读取IO
    增加 app/server/cache 目录，统一管理缓存逻辑（目前使用 cachetools）
    优化配置文件中的 cached 设置，增加过期策略（120秒过期）。

    